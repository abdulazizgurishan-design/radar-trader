// /api/monitor.js — ⛔ معطّل نهائياً (DISABLED)
// ════════════════════════════════════════════════════════════════════════
//  سبب التعطيل:
//   هذا الملف كان يخترع أهدافاً/وقفاً وهمياً (ATR ثابت 2% → وقف -1.6%)
//   ويتجاهل الأهداف الذكية المحفوظة من الرادار. وقفه الضيق يُضرب على أي
//   تذبذب طبيعي = خسائر على الضجيج.
//
//   إدارة الصفقات الآن مسؤولية /api/trade وحده (أوامر براكِت ذرّية:
//   دخول + هدف + وقف معاً، مع إدارة كل 5 دقائق). لا حاجة لهذا الملف.
//
//   أبقيناه موجوداً (بدل حذفه) ومُحيّداً: لو نُودي بالغلط (كرون قديم/رابط)،
//   يرجّع فوراً بلا تنفيذ أي أمر — صفر خطر.
//
//   ⚠️ لا تُعد تفعيله. أي إدارة جديدة تُضاف في /api/trade.
// ════════════════════════════════════════════════════════════════════════

export default async function handler(req, res) {
  return res.status(200).json({
    success: true,
    disabled: true,
    message: "monitor معطّل — إدارة الصفقات في /api/trade (أوامر براكِت). هذا الملف محيّد لا ينفّذ شيئاً.",
    actions: [],
  });
}
